"""Grammar analysis placeholders."""

from __future__ import annotations

from app.learning.content_schema import GrammarPattern


def analyze_grammar(text: str) -> list[GrammarPattern]:
    """Return grammar patterns from text in a future implementation."""

    return []
