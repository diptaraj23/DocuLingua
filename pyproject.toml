[project]
name = "doculingua"
version = "0.1.0"
description = "Streamlit MVP for generating static French learning guide PDFs from uploaded documents."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
